datasource db {
    provider = "postgresql"
    url      = "postgresql://postgres:root@localhost:5432/database"
}

model User {
    id        Int      @id @default(autoincrement())
    username  String
    password  String
    email     String
    createdAt DateTime @default(now())
}

generator client {
    provider = "prisma-client-js"
}
